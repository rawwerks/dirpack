---
description: Show dirpack plugin status (enabled / token budget / config file path)
---

The user wants the current status of the dirpack Claude Code plugin. Run the helper and show the raw output verbatim:

```bash
bash "${CLAUDE_PLUGIN_ROOT}/scripts/dirpack-cc-config.sh" status
```
